  <div class="row">
    <%= form_with model: book do |f| %>
      <h2 class="mt-3">
        New book
      </h2>
      <div class="field">
        <p>Title</p>
          <%= f.text_field :title %>
        <p>opinion</p>
          <%= f.text_area :body %>
      </div>
      <%= f.submit 'Create Book', class: 'btn btn-success btn-sm' %>
    <% end %>
  </div>